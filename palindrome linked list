class Solution:
    def isPalindrome(self, head: Optional[ListNode]) -> bool:
        p=head
        res=[]
        while p:
            res.append(p.val)
            p=p.next
        return res==res[::-1] 
        
